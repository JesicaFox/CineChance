---
phase: 22-my-movies-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/FilmGridWithFilters.tsx
  - src/app/my-movies/MyMoviesContentClient.tsx
autonomous: true
requirements:
  - UI-01
user_setup: []
must_haves:
  truths:
    - "User sees a scroll-to-top button on My Movies page when scrolled down >300px"
    - "Clicking scroll-to-top button smoothly scrolls to page top"
    - "Order numbers (1,2,3...) are NOT visible on movie cards on My Movies page"
    - "Order numbers remain visible on other pages using FilmGridWithFilters (backward compatible)"
    - "The hiding is non-destructive: showIndex prop can be set to true to restore numbers"
  artifacts:
    - path: "src/app/components/FilmGridWithFilters.tsx"
      provides: "showIndex prop support and conditional index passing"
      contains:
        - "showIndex?: boolean in FilmGridWithFiltersProps"
        - "index={showIndex ? index : undefined}"
    - path: "src/app/my-movies/MyMoviesContentClient.tsx"
      provides: "Scroll-to-top button functionality"
      contains:
        - "useState<boolean> for showScrollTop"
        - "useEffect with scroll event listener"
        - "Fixed position button with SVG arrow icon"
        - "FilmGridWithFilters with showIndex={false}"
  key_links:
    - from: "src/app/my-movies/MyMoviesContentClient.tsx"
      to: "window.scrollY"
      via: "scroll event listener updates showScrollTop state"
      pattern: "window\\.addEventListener.*scroll"
    - from: "src/app/components/FilmGridWithFilters.tsx"
      to: "src/app/components/MovieCard.tsx"
      via: "index prop conditional on showIndex"
      pattern: "index=\\{showIndex \\? index : undefined\\}"
---

<objective>
Add scroll-to-top button to My Movies page and hide serial order numbers on movie cards.

**Purpose:** Improve UX on long lists (scroll-to-top) and remove visual clutter (order numbers) while preserving ability to restore them later via `showIndex` prop.

**Output:** Modified FilmGridWithFilters.tsx (adds showIndex prop) and MyMoviesContentClient.tsx (adds scroll tracking + button, passes showIndex=false).
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/n0rds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-my-movies-ui-fixes/RESEARCH.md

# Source files to modify
@src/app/components/FilmGridWithFilters.tsx
@src/app/my-movies/MyMoviesContentClient.tsx

# Reference implementation (do NOT modify)
@src/app/search/SearchClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add showIndex prop to FilmGridWithFilters</name>
  <files>src/app/components/FilmGridWithFilters.tsx</files>
  <action>
    STEP 1: Add optional `showIndex?: boolean` prop to `FilmGridWithFiltersProps` interface.
      - Default value should be `true` to preserve existing behavior.
      - Add JSDoc comment: "Показывать ли порядковые номера на карточках фильмов"

    STEP 2: In the component function parameters, destructure `showIndex = true` from props.

    STEP 3: In the JSX where MovieCard is rendered (around line 357), change:
      From: `index={index}`
      To: `index={showIndex ? index : undefined}`

    STEP 4: Save file. Do NOT run tests yet (will be verified in final verification step).

    Reference: RESEARCH.md shows the exact pattern.
  </action>
  <verify>
    1. Run ESLint on modified file: `npm run lint` should pass (no new errors).
    2. Check that type `showIndex?: boolean` is present in FilmGridWithFiltersProps.
    3. Check that MovieCard call uses conditional index.
  </verify>
  <done>
    FilmGridWithFilters.tsx has showIndex prop (default true) and passes index conditionally to MovieCard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scroll-to-top button and hide order numbers on My Movies page</name>
  <files>src/app/my-movies/MyMoviesContentClient.tsx</files>
  <action>
    STEP 1: Add scroll tracking state and effect (copy pattern from SearchClient.tsx lines 37, 109-115):
      - Add `const [showScrollTop, setShowScrollTop] = useState(false);`
      - Add `useEffect` that adds window scroll listener:
        ```typescript
        useEffect(() => {
          const handleScroll = () => {
            setShowScrollTop(window.scrollY > 300);
          };
          window.addEventListener('scroll', handleScroll);
          return () => window.removeEventListener('scroll', handleScroll);
        }, []);
        ```

    STEP 2: Add scroll-to-top button JSX (copy from SearchClient.tsx lines 177-198) inside the return, after the BlacklistProvider / FilmGridWithFilters block:
      - Condition: `{showScrollTop && ( ... )}`
      - Button: fixed bottom-6 right-6, w-12 h-12, rounded-full, bg-blue-600, text-white, flex items-center justify-center, shadow-lg, hover:bg-blue-700, z-50
      - SVG icon: arrow up (same path as SearchClient)
      - onClick: `() => window.scrollTo({ top: 0, behavior: 'smooth' })`
      - aria-label: "Наверх"

    STEP 3: Pass `showIndex={false}` to FilmGridWithFilters component (around line 353):
      Find `<FilmGridWithFilters` and add prop: `showIndex={false}`

    STEP 4: Save file. Ensure there are no syntax errors.

    Note: The scroll button styling must match SearchClient exactly (same classes).
  </action>
  <verify>
    1. Run ESLint on modified file: `npm run lint` should pass.
    2. Check that showScrollTop state and scroll listener exist.
    3. Check that scroll-to-top button JSX is present with correct classes and SVG.
    4. Check that FilmGridWithFilters receives `showIndex={false}` prop.
  </verify>
  <done>
    MyMoviesContentClient.tsx has scroll-to-top button (shows at scrollY>300, smooth scroll to top) and hides order numbers via showIndex={false}.
  </done>
</task>

</tasks>

<verification>
**Final verification after both tasks:**

1. Run tests: `npm run test:ci` should pass, particularly `FilmGridWithFilters.orderNumbers.test.tsx` should still pass (since default showIndex=true in tests).
2. Manual check:
   - Navigate to /my-movies, scroll down, verify scroll-to-top button appears at bottom-right after 300px.
   - Click button, verify smooth scroll to top.
   - Verify no order numbers (1,2,3...) visible on movie cards.
   - Navigate to /search or another FilmGridWithFilters page, verify order numbers ARE visible there (backward compatible).
3. ESLint: `npm run lint` passes for both modified files.
4. Build: `npm run build` succeeds (if time permits).

If any verification fails, create gap closure plan.
</verification>

<success_criteria>
- Scroll-to-top button functional on /my-movies page
- Order numbers hidden on /my-movies page, visible elsewhere
- No regression in existing tests
- Code style conforms to project standards (ESLint pass)
</success_criteria>

<output>
After completion, create `.planning/phases/22-my-movies-ui-fixes/22-01-SUMMARY.md`
</output>
