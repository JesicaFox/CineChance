---
phase: 30-tdd-refactoring
plan: 03
type: execute
wave: 1
depends_on: [30-01, 30-02]
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "TypeScript: 0 errors"
    - "Tests: all pass, no regressions"
    - "Lint: passes"
  artifacts: []
---

<objective>
Верификация: TypeScript, тесты, lint
</objective>

<context>
@.planning/refactoring/tdd-refactoring-plan.md
</context>

<tasks>
<task type="auto">
  <name>Run TypeScript check</name>
  <files></files>
  <action>npx tsc --noEmit</action>
  <verify>0 TypeScript errors</verify>
  <done>TypeScript: 0 errors</done>
</task>

<task type="auto">
  <name>Run all tests</name>
  <files></files>
  <action>npx vitest run</action>
  <verify>All tests pass</verify>
  <done>Tests: no regressions</done>
</task>

<task type="auto">
  <name>Run lint</name>
  <files></files>
  <action>npm run lint</action>
  <verify>Lint passes</verify>
  <done>Lint: passes</done>
</task>
</tasks>

<verification>
All checks pass: tsc --noEmit, vitest run, npm run lint
</verification>

<success_criteria>
- TypeScript: 0 errors
- Tests: all pass, no regressions
- Lint: passes
</success_criteria>