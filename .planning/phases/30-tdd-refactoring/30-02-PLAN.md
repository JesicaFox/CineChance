---
phase: 30-tdd-refactoring
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/recommendation-algorithms/person-comparison.ts
  - src/lib/recommendation-algorithms/genre-twins.ts
  - src/lib/recommendation-algorithms/type-twins.ts
  - src/lib/recommendation-algorithms/drop-patterns.ts
  - src/app/api/recommendations/route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "person-comparison.ts has simplified nested ifs via early returns"
    - "genre-twins.ts and type-twins.ts share utility (DRY)"
    - "drop-patterns.ts has dead code removed"
    - "api/recommendations/route.ts uses zod schema validation"
  artifacts:
    - path: "src/lib/recommendation-algorithms/person-comparison.ts"
      provides: "Simplified with early return pattern"
    - path: "src/lib/recommendation-algorithms/genre-twins.ts"
      provides: "Uses shared twins utility"
    - path: "src/lib/recommendation-algorithms/type-twins.ts"
      provides: "Uses shared twins utility"
    - path: "src/lib/recommendation-algorithms/drop-patterns.ts"
      provides: "Cleaned exports, no dead code"
    - path: "src/app/api/recommendations/route.ts"
      provides: "Zod schema validation"
---

<objective>
Рефакторинг 5 средних файлов через TDD цикл
</objective>

<context>
@.planning/refactoring/tdd-refactoring-plan.md
@src/lib/recommendation-algorithms/person-comparison.ts
@src/lib/recommendation-algorithms/genre-twins.ts
@src/lib/recommendation-algorithms/type-twins.ts
@src/lib/recommendation-algorithms/drop-patterns.ts
@src/app/api/recommendations/route.ts
</context>

<tasks>
<task type="tdd">
  <name>Refactor person-comparison.ts</name>
  <files>src/lib/recommendation-algorithms/person-comparison.ts</files>
  <action>
    RED: Write test for early return pattern simplifying nested ifs
    GREEN: Apply early return pattern
    REFACTOR: KISS — simplify conditions
  </action>
  <verify>npx vitest run __tests__/recommendation-algorithms/</verify>
  <done>Nested ifs simplified, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor genre-twins.ts</name>
  <files>src/lib/recommendation-algorithms/genre-twins.ts</files>
  <action>
    RED: Write test for extracting shared logic with type-twins
    GREEN: Extract to shared utility
    REFACTOR: DRY — create shared/twins.ts
  </action>
  <verify>npx vitest run __tests__/recommendation-algorithms/</verify>
  <done>Logic shared, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor type-twins.ts</name>
  <files>src/lib/recommendation-algorithms/type-twins.ts</files>
  <action>
    RED: Write test for using shared twins utility
    GREEN: Refactor to use shared utility
    REFACTOR: DRY — complete shared twins pattern
  </action>
  <verify>npx vitest run __tests__/recommendation-algorithms/</verify>
  <done>Uses shared utility, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor drop-patterns.ts</name>
  <files>src/lib/recommendation-algorithms/drop-patterns.ts</files>
  <action>
    RED: Write test for used functions only
    GREEN: Remove dead code (YAGNI)
    REFACTOR: Clean up exports
  </action>
  <verify>npx vitest run __tests__/recommendation-algorithms/</verify>
  <done>Dead code removed, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor api/recommendations/route.ts</name>
  <files>src/app/api/recommendations/route.ts</files>
  <action>
    RED: Write test for simplified validation schema
    GREEN: Use schema validation (zod)
    REFACTOR: KISS — simplify validation logic
  </action>
  <verify>npx vitest run __tests__/api/</verify>
  <done>Validation simplified, tests pass, lint passes</done>
</task>
</tasks>

<verification>
After all 5 files: npm run lint && npx vitest run
</verification>

<success_criteria>
All 5 medium files refactored with TDD, tests pass, no lint errors
</success_criteria>