---
phase: 30-tdd-refactoring
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/app/recommendations/RecommendationsClient.tsx
  - src/lib/taste-map/compute.ts
  - src/lib/recommendation-algorithms/taste-match.ts
  - src/lib/recommendation-algorithms/person-recommendations.ts
  - src/lib/recommendation-algorithms/genre-recommendations.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "RecommendationsClient.tsx refactored with extracted hooks"
    - "compute.ts has separated functions with DRY utilities"
    - "taste-match.ts has normalized scores and constants"
    - "person-recommendations.ts shares logic with taste-match"
    - "genre-recommendations.ts shares logic with person-recommendations"
  artifacts:
    - path: "src/app/recommendations/RecommendationsClient.tsx"
      provides: "Refactored component with extracted hooks"
    - path: "src/lib/taste-map/compute.ts"
      provides: "Separated functions with DRY pattern"
    - path: "src/lib/recommendation-algorithms/taste-match.ts"
      provides: "Algorithm with DRY constants"
    - path: "src/lib/recommendation-algorithms/person-recommendations.ts"
      provides: "Algorithm with shared interface"
    - path: "src/lib/recommendation-algorithms/genre-recommendations.ts"
      provides: "Algorithm with DRY pattern"
---

<objective>
Рефакторинг 5 критичных файлов через TDD цикл (RED → GREEN → REFACTOR)
</objective>

<context>
@.planning/refactoring/tdd-refactoring-plan.md
@src/app/recommendations/RecommendationsClient.tsx
@src/lib/taste-map/compute.ts
@src/lib/recommendation-algorithms/taste-match.ts
@src/lib/recommendation-algorithms/person-recommendations.ts
@src/lib/recommendation-algorithms/genre-recommendations.ts
</context>

<tasks>
<task type="tdd">
  <name>Refactor RecommendationsClient.tsx</name>
  <files>src/app/recommendations/RecommendationsClient.tsx</files>
  <action>
    RED: Write test for extracting useFetchRecommendations and useSessionTracking as separate hooks
    GREEN: Minimal implementation — extract hooks to src/hooks/ directory
    REFACTOR: Apply KISS — simplify conditions, DRY — extract common functions
  </action>
  <verify>npx vitest run __tests__/hooks/</verify>
  <done>Component refactored, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor taste-map/compute.ts</name>
  <files>src/lib/taste-map/compute.ts</files>
  <action>
    RED: Write test for separate computeGenreProfile and computeGenreCounts functions
    GREEN: Split functions, DRY — extract normalizeGenreName to shared util
    REFACTOR: Apply YAGNI — remove unused parameters
  </action>
  <verify>npx vitest run __tests__/taste-map/</verify>
  <done>Functions separated, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor taste-match.ts</name>
  <files>src/lib/recommendation-algorithms/taste-match.ts</files>
  <action>
    RED: Write test for execute() with mocked getSimilarUsers
    GREEN: Minimal algorithm implementation
    REFACTOR: DRY — extract normalizeScores, weights to constants
  </action>
  <verify>npx vitest run __tests__/recommendation-algorithms/</verify>
  <done>Algorithm refactored, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor person-recommendations.ts</name>
  <files>src/lib/recommendation-algorithms/person-recommendations.ts</files>
  <action>
    RED: Write test for execute() with mocked getSimilarUsers
    GREEN: Minimal implementation
    REFACTOR: DRY — extract common logic with taste-match to shared/interface
  </action>
  <verify>npx vitest run __tests__/recommendation-algorithms/</verify>
  <done>Algorithm refactored, tests pass, lint passes</done>
</task>

<task type="tdd">
  <name>Refactor genre-recommendations.ts</name>
  <files>src/lib/recommendation-algorithms/genre-recommendations.ts</files>
  <action>
    RED: Write test for execute()
    GREEN: Minimal implementation
    REFACTOR: DRY — extract common logic with person-recommendations
  </action>
  <verify>npx vitest run __tests__/recommendation-algorithms/</verify>
  <done>Algorithm refactored, tests pass, lint passes</done>
</task>
</tasks>

<verification>
After all 5 files: npm run lint && npx vitest run
</verification>

<success_criteria>
All 5 critical files refactored with TDD, tests pass, no lint errors
</success_criteria>