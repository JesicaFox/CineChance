---
phase: 26-genre-profile-frequency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/taste-map/compute.ts
  - src/lib/__tests__/taste-map/compute.test.ts
autonomous: true
must_haves:
  truths:
    - "Diversity metric shows correct percentage (unique genres / 19)"
    - "GenreProfile remains unchanged (ratings-based) for other systems"
  artifacts:
    - path: "src/lib/taste-map/compute.ts"
      provides: "Corrected diversity calculation in computeMetrics()"
      min_lines: 1
    - path: "src/lib/__tests__/taste-map/compute.test.ts"
      provides: "Updated test expectations for diversity"
      min_lines: 1
  key_links:
    - from: "src/lib/taste-map/compute.ts::computeMetrics()"
      to: "src/app/profile/taste-map/TasteMapClient.tsx::computedMetrics.diversity"
      via: "computedMetrics returned from computeTasteMap()"
      pattern: "tasteMap.computedMetrics.diversity"
requirements:
  - DIV-01
---

<objective>
Fix the "Разнообразие" (Diversity) metric to correctly show percentage of unique TMDB genres (out of 19) that the user has watched.

Purpose: The current implementation incorrectly filters genres by rating threshold (>20) instead of counting unique genres. This fix corrects the metric to reflect actual genre diversity.

Output: Updated computeMetrics() function and updated tests.
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/n0rds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-genre-profile-frequency/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TMDB_GENRE_COUNT constant and fix computeMetrics diversity calculation</name>
  <files>src/lib/taste-map/compute.ts</files>
  <action>
    1. Add constant at top of file (after imports): `const TMDB_GENRE_COUNT = 19;`
    2. Locate `computeMetrics()` function (around line 259-279)
    3. Replace lines 274-276:
       FROM:
         const genreCount = Object.values(genreProfile).filter(v => v > 20).length;
         const diversity = Math.min(100, genreCount * 5);
       TO:
         const genreCount = Object.keys(genreProfile).length;
         const diversity = Math.round((genreCount / TMDB_GENRE_COUNT) * 100);
    4. Ensure comment above explains the logic: "Diversity: percentage of unique genres out of 19 TMDB genres"
  </action>
  <verify>
    Run: npm run test:ci | grep -A2 "computeMetrics"
    Verify: All tests pass, especially diversity calculation tests
  </verify>
  <done>
    - TMDB_GENRE_COUNT constant added
    - computeMetrics() uses Object.keys().length
    - diversity = round((count / 19) * 100)
    - Function signature unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update computeMetrics test cases</name>
  <files>src/lib/__tests__/taste-map/compute.test.ts</files>
  <action>
    1. Find test at line 282-286: "computes diversity based on genres with weight > 20"
    2. Update test description to: "computes diversity as percentage of unique genres"
    3. Update expectation:
       FROM: expect(result.diversity).toBe(10); // 2 genres > 20
       TO: expect(result.diversity).toBe(21); // 4 genres / 19 * 100 = 21.05 -> 21
       (genreProfile has Action, Drama, Comedy, Thriller = 4 unique)
    4. Find test at line 288-292: "caps diversity at 100 even with many genres"
    5. This test still valid (30 genres / 19 * 100 = 157 -> capped to 100 by Math.round? Actually: (30/19)*100 = 157.89, rounded = 158. Need to check if capping still happens)
       Actually: New formula: Math.round((30 / 19) * 100) = Math.round(157.89) = 158, which exceeds 100.
       We need to cap at 100? The requirement says percentage of 19, so max is 100. Should add Math.min(100, ...) or ensure rounding doesn't exceed.
       But 19/19*100 = 100 exactly, so any count >=19 should yield 100.
       So formula should be: Math.min(100, Math.round((genreCount / TMDB_GENRE_COUNT) * 100))
       OR: Math.round((Math.min(genreCount, TMDB_GENRE_COUNT) / TMDB_GENRE_COUNT) * 100)
       The simpler: Math.round((genreCount / TMDB_GENRE_COUNT) * 100) but cap at 100.
       Since genreCount can exceed 19 (multiple occurrences don't increase unique count actually - Object.keys returns unique, so count <= number of unique genres from user movies, but theoretical max is 19. So can't exceed 19? Actually yes, if user has all 19 genres, count = 19, (19/19)*100 = 100. If somehow count > 19 (shouldn't happen with unique keys from genreProfile), we should still cap. So safer: Math.min(100, Math.round(...))
    6. Update test expectation: expect(result.diversity).toBe(100);
    7. Optional: add test case for exactly 19 genres -> 100%
  </action>
  <verify>
    Run: npx vitest run src/lib/__tests__/taste-map/compute.test.ts
    Verify: All tests pass, including new expectations
  </verify>
  <done>
    - Test description updated
    - Expected value changed from 10 to 21
    - Cap test expectation remains 100 (or updated if new formula differs)
    - Optional edge case test added
  </done>
</task>

</tasks>

<verification>
1. Diversity calculation: (unique_genre_count / 19) * 100, rounded
2. TMDB_GENRE_COUNT = 19 defined in compute.ts
3. Tests updated and passing: npm run test:ci
4. UI displays diversity correctly (no code change needed in UI)
5. Other systems unaffected (genreProfile unchanged)
</verification>

<success_criteria>
- computeMetrics() returns correct diversity percentage
- All tests pass
- No regression in other features
</success_criteria>

<output>
After completion, create `.planning/phases/26-genre-profile-frequency/26-01-SUMMARY.md`
</output>
